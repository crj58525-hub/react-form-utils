{
  "name": "react-form-utils",
  "version": "1.2.0",
  "description": "Lightweight React form validation utilities",
  "main": "src/index.js",
  "scripts": {
    "prepare": "husky",
    "lint": "eslint src/",
    "test": "jest"
  },
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "eslint": "^8.56.0",
    "jest": "^29.7.0"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "jest --findRelatedTests"]
  }
}
